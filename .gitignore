__pycache__/
*.pyc
.DS_Store
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*Notes.bib
paper/*.synctex.gz
paper/arxiv-submission.tar.gz
.omc/
.venv-braket/

# Pre-reseed snapshot of results/, kept locally as the comparison base
# for compare_prehash.py. Every paired old/new value it supports is
# preserved in results/prehash_comparison.json, which IS tracked.
results_prehash/
